using System;
using System.Collections.Generic;

namespace Strings
{
    public class Regex
    {
        //private static Dictionary<char, Func<char, char, bool>> Roots = new Dictionary<char, Func<char, char, bool>>
        //{
        //    { '.', (a,b) => { return true; } } // "." matches ANY character => returns true.
        //    { '*', (a,b) => { return  } }
        //};

        /// <summary>
        /// 
        /// </summary>
        /// <param name="s"></param>
        /// <returns></returns>
        public static List<string> SplitIntoRoots(in string s)
        {
            List<string> roots = new List<string>();

            

            return roots;
        }
    }
}
